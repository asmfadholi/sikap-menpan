import { css } from "@emotion/react";

export const styIconElipsis = css`
	cursor: pointer;
	color: #fbc833;
	font-size: 25px;
`;

export const styEdit = css`
	font-style: normal;
	font-weight: normal;
	font-size: 14px;
	color: #535c65;
`;
