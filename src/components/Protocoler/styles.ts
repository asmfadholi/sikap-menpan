import { css } from "@emotion/react";

export const styTable = css`
	.ant-table-tbody .ant-table-cell {
		font-style: normal;
		font-weight: normal;
		font-size: 14px;
		line-height: 16px;
		color: #535c65;
	}
`;
